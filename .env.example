# Backend Environment Variables

# MongoDB connection string (replace with your MongoDB Atlas URL)
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/productstore?retryWrites=true&w=majority

# Server port (optional, defaults to 5000)
PORT=5000

# Node environment
NODE_ENV=production
